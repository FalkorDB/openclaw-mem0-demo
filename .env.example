# OpenAI API key (required for embeddings, entity extraction, and LLM)
OPENAI_API_KEY=sk-your-key-here

# Mem0 API key (only needed for Platform Mode — not needed for OSS mode)
# MEM0_API_KEY=m0-your-key-here
