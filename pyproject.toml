[project]
name = "openclaw-mem0-demo"
version = "0.1.0"
description = "Mem0 + FalkorDB graph-structured agent memory demo"
requires-python = ">=3.10"
dependencies = [
    "mem0ai>=0.1.0",
    "mem0-falkordb>=0.1.0",
    "python-dotenv>=1.0.0",
]
